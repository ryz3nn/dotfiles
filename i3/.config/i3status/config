general {
  colors = true
  interval = 5
}

order += "load"
order += "memory"
order += "tztime local"

load {
  format = "CPU: %1min"
}

memory {
  format = "RAM: %used/%total"
}

tztime local {
  format = "%Y-%m-%d %H:%M:%S"
}
